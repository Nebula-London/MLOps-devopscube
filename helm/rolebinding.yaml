apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding

metadata:
  name: airflow-admin

subjects:
- kind: ServiceAccount
  name: airflow-runner
  namespace: airflow

roleRef:
  kind: ClusterRole
  name: cluster-admin
  apiGroup: rbac.authorization.k8s.io